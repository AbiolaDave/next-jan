export type Product = {
  id: number;
  image: string;
  title: string;
  description: string;
  price: number;
};

export type User = {
  id: number;
  email: string;
  username: string;
  password: string;
};